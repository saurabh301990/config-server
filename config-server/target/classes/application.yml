spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saurabh301990/MicroService_Eureka_Example
          clone-on-start: true

server:
  port: 9296
